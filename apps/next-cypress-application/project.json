{
  "name": "next-cypress-application",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/next-cypress-application",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/next-cypress-application",
        "outputPath": "dist/apps/next-cypress-application"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/next-cypress-application"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "next-cypress-application:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "next-cypress-application:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "next-cypress-application:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "next-cypress-application:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/next-cypress-application/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "apps/next-cypress-application/**/*.{ts,tsx,js,jsx}"
        ]
      }
    }
  },
  "tags": []
}
